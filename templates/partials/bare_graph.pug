.bar-container
  .entry.empty
  -for entry in graph.entries.reversed
    .entry(class="{% cycle 'odd' 'even' %}", style='height: {{ entry.years }}%; width: {{ entry.percentage }}%;')
      .persons(class="{% cycle 'odd' 'even' %}", style='width: {{ entry.person_percentage }}%') &nbsp;

